# Optional override for API origin (scheme + host, no trailing slash).
# Default in production: https://api.findgarageandyardsale.com
# In dev, leave unset to use Vite proxy to that host.
# VITE_API_BASE_URL=https://api.findgarageandyardsale.com
