# API Base URL for production
# Leave empty to use relative paths (requires server proxy - see DEPLOYMENT.md)
# Or set to full API URL if backend has CORS enabled for your domain
VITE_API_BASE_URL=

# Example if using direct API (backend must allow CORS from your domain):
# VITE_API_BASE_URL=http://18.232.33.127:8000
